{
  "name": "biohacker-root",
  "private": true,
  "scripts": {
    "build": "cd BioHacker && npm run build",
    "vercel-build": "cd BioHacker && npm run build"
  },
  "workspaces": [
    "BioHacker"
  ],
  "dependencies": {
    "sonner": "^2.0.7"
  }
}